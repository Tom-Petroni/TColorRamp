[package]
name = "tcolorramp-nuke"
version = "1.0.0"
edition = "2024"
links = "DDImage"
publish = false

[lib]
name = "tcolorramp_nuke"
crate-type = ["cdylib"]

[dependencies]

[build-dependencies]
cc = "1.2"
